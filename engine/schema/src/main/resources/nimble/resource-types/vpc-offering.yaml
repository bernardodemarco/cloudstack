# Licensed to the Apache Software Foundation (ASF) under one
# or more contributor license agreements.  See the NOTICE file
# distributed with this work for additional information
# regarding copyright ownership.  The ASF licenses this file
# to you under the Apache License, Version 2.0 (the
# "License"); you may not use this file except in compliance
# with the License.  You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing,
# software distributed under the License is distributed on an
# "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
# KIND, either express or implied.  See the License for the
# specific language governing permissions and limitations
# under the License.
tosca_definitions_version: tosca_2_0

description: >
  VpcOffering node type definition.

data_types:
  NetworkServiceProviderMapping:
    properties:
      service:
        type: string
        description: The network service.
        required: true
      provider:
        type: string
        description: The network service provider.
        required: true

node_types:
  VpcOffering:
    metadata:
      provisioning-api: createVPCOffering
      rollback-api: deleteVPCOffering
    properties:
      name:
        type: string
        description: Name of the VPC offering.
        required: true
      display-text:
        type: string
        description: Display text of the VPC offering.
        required: false
        metadata:
          api-parameter: displaytext
      zone-id:
        type: string
        description: The ID of the availability zone. If not specified, the VPC offering will be public.
        required: false
        metadata:
          api-parameter: zoneid
      domain-id:
        type: string
        description: The ID of the domain for which the VPC offering will be available. If not specified, the compute offering will be public.
        required: false
        metadata:
          api-parameter: domainid
      enable:
        type: boolean
        description: Whether the VPC offering will be enabled during creation.
        required: false
      internet-protocol:
        type: string
        description: "The internet protocol of the offering. Valid values are 'ipv4' and 'dualstack'."
        validation: { $valid_values: [ $value, [ ipv4, dualstack ] ] }
        required: false
        metadata:
          api-parameter: internetprotocol
      supported-services:
        type: list
        entry_schema:
          type: string
        description: The network services supported by the offering.
        required: false
        metadata:
          api-parameter: supportedservices
      service-provider-list:
        type: list
        entry_schema:
          type: NetworkServiceProviderMapping
        description: The network services mapped to their providers.
        required: false
        metadata:
          api-parameter: serviceproviderlist
      service-offering-id:
        type: string
        description: The ID of the network service offering for the VPC router.
        required: false
        metadata:
          api-parameter: serviceofferingid
    attributes:
      id:
        type: string
        description: The ID of the VPC offering.
      zone-name:
        type: string
        description: The name of the zone the VPC offering belongs to.
        metadata:
          api-response-attribute: zone
      domain-name:
        type: string
        description: The domain name associated with the VPC offering.
        metadata:
          api-response-attribute: domain
